<%= form_for(tbdatum) do |f| %>
  <% if tbdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tbdatum.errors.count, "error") %> prohibited this tbdatum from being saved:</h2>

      <ul>
      <% tbdatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :level %>
    <%= f.text_field :level %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_area :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
