<%= form_for(@koman, remote: true) do |f| %>
  <% if @koman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(koman.errors.count, "error") %> prohibited this koman from being saved:</h2>

      <ul>
      <% koman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col-md-6">
      <%= f.label :name %>
      <%= f.text_field :name, class:'form-control' %>
    </div>
  </div>

  <div class="row">
    <div class="field col-md-6">
      <%= f.label :comment %>
      <%= f.text_area :comment, class:'form-control' %>
    </div>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
